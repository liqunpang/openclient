pipeline {
  stages {
      stage('Test') {
          steps {
              sh 'echo $PATH; which docker ;sleep 50000'
          }
      }
  }


    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
